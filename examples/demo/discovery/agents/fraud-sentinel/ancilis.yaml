agent:
  name: fraud-sentinel
  description: CLI risk agent scoring card transactions and identity signals from batch jobs
  owner: risk-team
my_agent_handles:
  - credit_cards
  - financial_records
  - personal_info
certification_targets:
  - aiuc-1
security:
  mode: enforce
  tools:
    allowed:
      - score_transaction
      - get_fraud_rules
      - flag_suspicious
      - get_account_history
      - verify_identity
    blocked:
      - override_fraud_flag
      - bulk_export_scores
